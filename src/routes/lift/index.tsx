import { component$, useSignal, $ } from "@builder.io/qwik";

  

const Top = component$(() => {
    return (
        <div>
        mid
        </div>
    );
});
  

export default component$(() => {
  return (
    <div>
      top
    </div>
  );
});
